# Project: mTLS Service Mesh

*Coming soon.*

Two services that authenticate each other using mutual TLS. Generate certificates for each service, configure both as TLS clients and servers, reject unauthorized callers.
