# Project: Certificate Authority

*Coming soon.*

Build a mini CA: generate a root certificate, sign server certificates, verify certificate chains. Use `rcgen` for generation and `x509-parser` for validation.
