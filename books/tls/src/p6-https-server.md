# Project: HTTPS Server

*Coming soon.*

Serve a web page over TLS using `tokio-rustls`. Generate a self-signed certificate, configure the TLS acceptor, handle HTTP requests, return HTML. Test with a real browser.
