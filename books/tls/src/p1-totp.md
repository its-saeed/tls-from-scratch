# Project: TOTP Authenticator

*Coming soon.*

Build a TOTP (Time-based One-Time Password) generator — the algorithm behind Google Authenticator. Uses HMAC-SHA1 + Unix timestamp to generate 6-digit codes that change every 30 seconds.
