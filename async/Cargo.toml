[package]
name = "async-lessons"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
mio = { version = "1", features = ["os-poll", "net"] }
tokio = { version = "1", features = ["full"] }
